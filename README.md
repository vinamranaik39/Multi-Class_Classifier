# Multi-Class_Classifier
This project implements a PyTorch-based Multi-Class digit classifier using a fully connected neural network with two hidden layers. It includes GPU support, reproducible training, loss tracking, model evaluation, prediction, visualization, and model saving, providing a complete end-to-end deep learning classification pipeline.
